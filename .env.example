# Environment Variables - IMPORTANT: Store these in GitHub Secrets, NOT in files!
# See: Repository Settings → Secrets and variables → Actions

# GitHub Secrets Names (set these in your repo):
# - VITE_SERVICE_ID (your EmailJS service ID)
# - VITE_TEMPLATE_ID (your EmailJS template ID)  
# - VITE_PUBLIC_KEY (your EmailJS public key)

# Local Development Only (if you need to test locally):
VITE_SERVICE_ID=your_service_id_here
VITE_TEMPLATE_ID=your_template_id_here
VITE_PUBLIC_KEY=your_public_key_here

# NOTE: Never commit .env.local to git if it contains real secrets!
